# Handoff — 2026-07-05 00:06

## Snapshot

- **Branch:** main, ren og synket. **0 åpne PR-er, 0 uncommitted** (unntatt evt. `apps/api/.env.example` — se Blocked).
- **Last commit:** 9038b88 `feat(mobile): toppliste med tre visninger … (#83)`
- **Uncommitted changes:** nei
- **Session length:** ~12 t (maraton, fredag kveld → natt til søndag; Ludvig testet live på mobil via LAN)
- **Claude model:** Fable 5 (1M context)

## What got done this session

**12 PR-er merget (#72–#83).** Tre bolker:

1. **Ytelses-runde** (fra multi-agent-audit av hele appen — funn verifisert adversarielt):
   - #72 hjem-katalogen → FlashList + memoized `KnuteListCard` (id-basert onPress); søk re-rendrer ikke lista
   - #73 feed: `initialNumToRender 1 / windowSize 5 / maxToRenderPerBatch 2` + memo `FeedCard`
   - #74 toppliste: `myPlaceY` state→ref + memo rader (senere erstattet av full redesign)
   - #76 `GET /pending` cursor-paginert (20/50, feed-kontrakten) + NY `GET /pending/count` for badgene; mobile `useInfiniteQuery` + dedupe
   - #79 bilde-variant-søm: `BUNNY_OPTIMIZER_ENABLED`-flagg (av!) → `publicUrlForKey(key, origin, variant)`; `/pending` ber om `width=750&quality=80`. Kjøp av Bunny Optimizer (~$9,5/mnd) ligger på LANSERINGS-sjekklista
   - #80 småplukk: tab-barene bruker `router.replace` (+ aktiv-fane no-op), send-inn oppskalerer aldri bilder, splash-farge russ-rød i app.json
2. **Docs/beslutninger:**
   - #75 natt-handoffen + `docs/cost-model.md` (ferske juli-priser; erstatter architecture.md §5–6-tall)
   - #77 **ADR-0011 AKSEPTERT**: 100+ skoler 2027. **INGEN pilotfase — v1 VAR piloten** (Ludvig korrigerte hardt; aldri si «pilot» om 2027-lanseringen)
3. **Toppliste-epicen (design + klasser):**
   - #81 toppliste i sticker-design: medaljer (gull/sølv/bronse-tokens NYE i theme), rangtitler (v1 §6), «Deg»-chip. Etter mobiltest: festet «min plass»-kort FJERNET, header = kun sentrert «Toppliste»
   - #82 backend: `school_classes`-tabell (full RLS+FORCE, migrasjon 0018+0019, SAFE, Ludvig-OK) + `users.class_id` + `className` i leaderboard-payload + cross-tenant-join-test
   - #83 tre visninger: **Skolen · Klassen min · Klassekamp** + drill-inn (variant A: trykk Klassekamp-rad → klassens egen liste m/ kontekstbanner). Ludvig så A live og likte den (B=chip-rad var fallback, ble ikke nødvendig)

## What's in progress

None — alt er merget og main er grønn.

## What's blocked / waiting

- **Ludvig manuelt:** legg `BUNNY_OPTIMIZER_ENABLED=false` inn i `apps/api/.env.example` (fila ligger bak `.env*`-permission-sperren, Claude kan ikke)
- **Brother-review i etterkant:** #76, #79, #82 har `etter-review-bror`-label (avtalen mens han er borte til ~midt-juli)

## What's next when you come back

1. **Profil-skjermen i sticker-design** — SISTE skjerm i design-epicen (ADR-0017). Husk `/knuteloop-design` først. Vurder «Velg klasse»-felt der (claim-flyten; klassene ligger klare i `school_classes`)
2. architecture.md §6-rampa skrives om (ADR-0011 krever det — «pilot»-språket skal ut)
3. Backlog fra perf-runden: composite cursor `(created_at, id)` for feed+pending; `limit(500)`-caps på knuter/leaderboard; blurhash-placeholder (trenger backend-pipeline); **transaksjon-per-request-fiksen (ADR-0011s #1 throughput-blocker, stor jobb, skalerings-epic)**
4. Toppliste-toppliste: FlashList-jest-oppsett mangler → skjermen har ingen render-tester (kun logikk-testene i `lib/leaderboard-ui.test.ts`)

## Gotchas / things to remember

- **ALDRI testfiler i `apps/mobile/app/`** — Expo Router laster ALT der som ruter; en `.test.tsx` krasjet appen på boot («Can't find variable: jest»). Tester bor i `components/` eller `lib/`
- **Klassekamp = SNITT, og tallet som vises ER tallet det rangeres på** (v1-buggen: viste sum, rangerte snitt). Låst med test i `lib/leaderboard-ui.test.ts`
- **Klasse-modellen:** knutesjef oppretter klasser (`school_classes`), elever claimer via `users.class_id` (knappevalg ved innlogging når auth kommer; evt. profil-skjermen før det)
- **Stacked PR + squash-merge = GitHub LUKKER barnet** i stedet for å retarget (skjedde med #78 → gjenskapt som #79 via cherry-pick)
- **TaskStop dreper ikke barne-prosesser på Windows** — node-orphans holdt port 3000/8081. Fiks: `netstat -ano | grep :3000` → `taskkill //F //PID <pid>`
- **Dev-serveren for mobiltest:** `EXPO_PUBLIC_API_URL=http://<LAN-IP>:3000 pnpm dev:all` (IP-en var 192.168.86.23; sjekk `ipconfig`). Web-link til telefonen: `http://<LAN-IP>:8081`
- Bildene komprimeres til ~200–400 KB (ikke 1 MB) — kostmodellens «5×»-frykt var reelt ~1,5–2,5×; feed i fullskjerm er riktig størrelse allerede
- `submissions.ts` er ~383 linjer — NESTE endring der skal splitte ut review-delen

## Files touched

Hele lista er PR-ene #72–#83 på GitHub (12 squash-commits på main siden 9d820ad). Kjerneområder: `apps/mobile/app/{index,feed,leaderboard,review}.tsx`, `apps/mobile/components/{AppTabBar,KnutesjefTabBar,knute/KnuteListCard}.tsx`, `apps/mobile/lib/{api,theme,leaderboard-ui}.ts`, `apps/api/src/routes/{submissions,leaderboard}.ts`, `apps/api/src/lib/storage.ts`, `apps/api/src/db/schema/{school-classes,users}.ts` + migrasjoner 0018/0019, `apps/api/scripts/dev-setup.ts`, `docs/{cost-model.md,adr/0011}`.

## Resume command

```
git checkout main && git pull
pnpm install
pnpm typecheck && pnpm test
# For mobiltest: EXPO_PUBLIC_API_URL=http://<LAN-IP>:3000 pnpm dev:all
```

## Open questions for Ludvig

- Skal «Velg klasse» inn på profil-skjermen (før auth-epicen), eller vente på innloggings-flyten?
- Klassekamp: skal klasser under N russ skjules/merkes (1-russ-klasser vinner lett på snitt)? Ikke noe problem med dev-seeden, men verdt en tanke før ekte data.

## Related ADRs

- **ADR-0011 flippet til Accepted** (#77) — 100+ skoler 2027, ingen pilotfase. Krever architecture.md §6-oppdatering (utestående)

## Tests status at session end

- Typecheck: ✅ (alle workspaces)
- Lint: ✅
- Unit tests: ✅ mobile 72/72
- RLS integration tests: ✅ api 197/197 (inkl. ny cross-tenant-join-test for school_classes)
- Notes: ingen kjente feil; dev-DB reseedet med klasser (3STA/3MKA/3PBA)
