# Handoff — 2026-07-28 00:29

> Skrevet rett før Ludvig drar i militæret (tirsdag 28. juli), borte ~3 uker.
> Denne økta handlet mest om å RIGGE ting for at Brage skal kunne jobbe alene
> mens Ludvig er borte — ikke om å bygge produkt.
>
> **NB: Se «## Tillegg» nederst** — mer arbeid ble gjort samme natt etter at
> denne handoffen først ble skrevet (dev-arbeidsflyten + produktideene).
> Siste dev-commit ved øktslutt: **`b62ecf5`** (PR #124). `main` fortsatt `1c0b896`.

## Snapshot

- **Branch:** `dev` (NY default branch — se under). Lokalt arbeidstre står her.
- **Last commit (main):** `1c0b896` chore(dev): dev-arbeidsflyt mens Ludvig er borte (#123)
- **Uncommitted changes:** Nei (rent tre). `dev` == `main` == `1c0b896`.
- **Session length:** ~lang dagsøkt (Brage kom om bord, mye oppsett)
- **Claude model:** Fable 5 (deler av økta: Opus 4.8 «opus[1m]»)

## What got done this session

**Brage om bord (Ludvigs kompis, GitHub `BragePR`, koder med Codex):**
- Invitert som collaborator (write). Repoet er OFFENTLIG til ~okt/nov 2026.
- `AGENTS.md` skrevet om fra «design-hjelper» til ekte frontend-bidragsyter
  (feature-scope, PR-flyt, primitives, ADR-0023) — #109.
- `docs/onboarding.md` + `docs/ai-arbeidsmetoder.md` — maskinoppsett + arbeidsvaner (#110).

**Merge-toget (6 upushede brancher fra 24. juli → main):** #103–#108 (docs-vask,
App Store-bibliotekvask 186→111, streak fjernet, dagens-knute-endepunkt,
branded ids, elev-hjem + fem-fane tabbar).

**Dev-ergonomi-fikser:** #111 knutesjef-fane helt til høyre (rollegated),
#112 stabile seed-ider + 30d dev-tokens, #113 401-selvheling i dev-klienten.
Disse løste «Invalid token hver økt» + at knutesjef-panelet «forsvant».

**Produkt/strategi (docs, ikke kode):**
- #115 `docs/produktnotat-eksamensro.md` — eksamensroen EIES, ikke bekjempes
  (bekreftet uavhengig av både rådgiver og Claude).
- #116 ADR-0024 (**Proposed**) rolig streak med metningskurve (Ludvig+Brage vil
  ha streak TILBAKE, men uten skam/press; ikke bygg før Accepted).
- #117 backend-enabler: `visibility` i pending-køen (todelt godkjenn-kø).
- #120 animasjonslinja klargjort: bevegelse er VELKOMMEN, kun evige idle-loops
  forbudt («levende ved ankomst, rolig etterpå»).

**Brage-køen (issues):** #114 dagens-knute-kort (førsteutkast reviewet),
#118 todelt godkjenn-kø, #119 pending-teller på fanen, #121 del/privat-toggle,
#122 ankomst-animasjon som hook (etter #114).

**DEV-ARBEIDSFLYT (hovedgrepet denne økta — #123 + GitHub-oppsett):**
- `dev`-branch opprettet fra main og gjort til **default branch**.
- CI trigges nå på PR-er mot både `main` og `dev`.
- Rulesets: `protect-default-branch` (id 19774230, ~DEFAULT = dev nå) verner dev;
  nytt `protect-main` (id 19854023) verner main. Begge: PR + grønn CI, 0 godkj.
- Brage brancher fra dev, PR mot dev, **self-merger på grønn CI** (venter aldri
  på et menneske — løser v1-smerten). main er fryst for Ludvigs review.

## What's in progress

- **#114 dagens-knute-kort** (Brages branch `feat/dagens-knute-kort`, PUSHET, INGEN
  PR ennå). Førsteutkast bygger grønt (26 suiter/106 tester). Review gitt: (1) de tre
  evige loop-animasjonene skal kjøre ÉN gang ved ankomst i stedet, (2) `isCompleted`
  er sann også for `rejected` — avvist dagens-knute skal vise aktivt kort igjen. Det
  visuelle (gullramme, `daily`-farge, «Ta knuten»-knapp) er GODKJENT. Brage må rebase
  branchen på `dev` (den er cuttet fra eldre main; mulig liten konflikt i `lib/api.ts`).

## What's blocked / waiting

- **ADR-0024 (rolig streak):** Proposed. Åpne spørsmål (reset vs. mykt forfall,
  CAP/τ-tall, visning, norsk navn) avgjøres i designøkt NÅR Ludvig er tilbake.
  Ikke bygg streak før den er Accepted.
- **Badges:** fortsatt parkert (ADR-0023) — venter felles designøkt.
- **Brage aksepterer GitHub-invitasjon** (hvis ikke gjort) før han kan pushe.

## What's next when you come back

1. **Kjør `/comeback`.**
2. **Nullstill dev-arbeidsflyten** — se `docs/dev-arbeidsflyt-away.md`: gå gjennom
   `dev` commit-for-commit, merge `dev → main`, sett `main` som default igjen,
   fjern det midlertidige banneret øverst i `AGENTS.md`, slett `dev` + guide-fila.
3. **Review Brages PR-er** (det han rakk på ferien — sannsynligvis ~1 uke koding).
4. **Designøkt: ADR-0024 rolig streak** (avgjør de åpne spørsmålene) + badges når du vil.

## Gotchas / things to remember

- **`dev` er default branch nå** — nye brancher/PR-er peker dit. Dette er MIDLERTIDIG.
- **PATCH på GitHub rulesets gir 404 i dette repoet** — bruk POST (create) / DELETE,
  ikke PATCH. (Derfor ble main vernet med et NYTT ruleset i stedet for å endre det gamle.)
- **Animasjonslinja:** ikke flagg bounded animasjoner som ADR-brudd. Kun evige
  `withRepeat(..., -1)` på oppmerksomhetseffekter er forbudt (skeleton-shimmer unntatt).
- **dev-tokens varer nå 30d + seed-ider er stabile** — «Invalid token» skal være utryddet.
- **Seed endret (111 knuter, 0 tekst/18+)** — kjør `pnpm dev:setup` etter pull.
- Brage-seed-brukeren «Brage» (knutesjef, Hetland) fantes før ekte Brage — tilfeldig.

## Files touched

Denne økta var mange små PR-er (#103–#123) + GitHub-config (rulesets, default branch,
collaborator-invitasjon, 5 issues) + minne. Ingen ucommitede filer. Se `git log`.

## Resume command

```
git fetch origin
git checkout dev            # eller main — begge står på 1c0b896
# les docs/dev-arbeidsflyt-away.md FØR du merger noe
pnpm install
pnpm typecheck && pnpm lint && pnpm test
```

## Open questions for Ludvig

- ADR-0024: hard reset eller mykt forfall på streaken? CAP/τ-tall? Navn?
- Betalt Claude-review-i-CI (koster penger, krever API-nøkkel) — verdt det for å
  dekke review-gapet mens du er borte? (Droppet nå; kan vurderes senere.)
- `design/calm-mobile` (gammel Codex-branch) — droppe eller redde noe fra?

## Related ADRs

- ADR-0024 (Proposed) — rolig streak med metningskurve.
- ADR-0023 (Accepted) — presisert i #120: forbyr feiringslag, ikke bevegelse.
- ADR-0021/0022 — visibility-enabler #117 (todelt godkjenn-kø).

## Tests status at session end

- Typecheck: **pass**
- Lint: **pass**
- Unit/integration (main): **pass** (API 247 + mobil 106; CI grønn på alle #103–#123)
- RLS integration tests: **pass**
- Notes on test failures: Ingen. Brages `feat/dagens-knute-kort` bygger også grønt
  (26 suiter/106) men har utestående review-fikser før PR.

---

## Tillegg (samme natt, etter at handoffen ble skrevet)

**1. Dev-arbeidsflyten er RIGGET (#123 + GitHub-config).** Se hovedhandoffen +
`docs/dev-arbeidsflyt-away.md` for full nullstilling. Kort:
- `dev` er nå **default branch** og Brages trunk. Han self-merger på grønn CI.
- `main` er fryst (ruleset `protect-main`, id 19854023). `dev` vernet av
  `protect-default-branch` (id 19774230, ~DEFAULT = dev). CI kjører på begge.
- **NB: PATCH på GitHub rulesets gir 404 i dette repoet — bruk POST/DELETE.**

**2. Demo-eksperimentet er høstet (#124, på dev).** Ludvig testet Fable 5 vs Opus
med en åpen «lag Knuteloop-demo»-oppgave. Claude høstet ideene til
`docs/produktideer-demo-eksperiment.md` (meny med calm-app-flagg) — Brage tar
stilling i **issue #125**. Peek-prinsippet «ark for å se, side for å gjøre» er
skrevet inn i `frontend.md` §6 (Ludvig-endorset). Beste nye ideer: **tips per
knute**, «X har fullført», knutesjef-pin i feeden.

**3. Korreksjon fra Ludvig:** **russefeiringen er flyttet** — INGEN 17.mai-
nedtelling, ingen «mai-topp»-antakelser. Berører `architecture.md` §6 +
eksamensro-notatet (oppdater når du er tilbake). Lagret som minne.

**4. Brage-køen ved øktslutt:** #114 (dagens-knute-kort, reviewet, venter PR),
#118, #119, #121, #122 (kode) + #125 (produktideer, «ta stilling»). Alt mot `dev`.

**Åpne spørsmål lagt til:** Vil du grave dypere i demoene? Begge filene ble kuttet
~50k tegn ved innliming — gi Claude filstiene for å lese resten.

**Første jobb når du er tilbake:** `/comeback` → les `docs/dev-arbeidsflyt-away.md`
→ review dev → merge `dev → main` → sett `main` som default igjen → fjern
AGENTS-banneret. Deretter designøkt ADR-0024 + produktideene (#125).
