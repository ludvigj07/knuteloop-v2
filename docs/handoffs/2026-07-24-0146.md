# Handoff — 2026-07-24 01:46

## Snapshot

- **Branch:** `design/calm-mobile`
- **Last commit:** `bcfae17 fix(api): schools read-only for app_role + dup-check tenant filter (review follow-ups) (#102)`
- **Uncommitted changes:** Yes — Codex changed `apps/mobile/app/profile.tsx` and created this handoff. Pre-existing Claude/user changes remain in `.claude/rules/frontend.md`, `.claude/settings.json`, `.gitignore`, `AGENTS.md`, `apps/mobile/CLAUDE.md`, `docs/adr/README.md`, `docs/architecture.md`, `.agents/`, `.codex/`, `docs/adr/0023-calm-app-no-celebration-layer.md`, `docs/app-store-content-review.md`, older handoffs, `docs/nattplan-2026-07-24.md`, and `docs/spotify-for-knuter-plan.md`.
- **Session length:** ~30 minutes
- **Codex model:** Codex (exact model name was not exposed in the session)

## What got done this session

- Read the v1 repository as product archaeology and mapped its core surfaces against v2: knuter, innsending, feed, toppliste, profiler, knutesjef/admin, moderation, and the old gamification layer.
- Read and applied `.claude/skills/knuteloop-design`: retained the cream-paper sticker identity, royal blue, restrained yellow accent, Bricolage/Inter/JetBrains Mono hierarchy, and tactile press feedback. Ludvig's newer calm-app decision overrides the skill's stale streak/confetti/badge guidance.
- Created `design/calm-mobile`; all pre-existing uncommitted Claude/user changes were carried over untouched.
- Removed the visible streak from `apps/mobile/app/profile.tsx` and replaced it with the useful count of completed knuter.
- Simplified duplicate profile statistics: gullknuter are now quiet section metadata instead of a second large stat strip.
- Changed «Knote-kategorier» to correct Bokmål «Knutekategorier».
- Made «Andre på appen» entries open the already-existing public profile route, with link role, Bokmål label/hint, haptic press feedback, and reduced-motion support inherited from the shared `Pressable`.

## What's in progress

- File: `apps/mobile/app/profile.tsx`
- What's done in this file: Calm profile summary, no streak UI, tappable public-profile links, accessible section headings.
- What's not done: Visual device/browser QA could not be completed because the Codex in-app browser connection failed before opening the app.
- What state is it in: Compiles, lints, and all mobile tests pass; no TODO markers were added.

## What's blocked / waiting

- The Codex in-app browser failed with a missing sandbox metadata error. This was a tool-connection problem, not an app/runtime failure. Visually inspect the profile on a phone/simulator before merging.
- The API response type still contains `streak` in `apps/mobile/lib/api.ts`. Codex was explicitly forbidden from touching API/data files; Claude or the backend reviewer must remove the data contract and backend logic separately if streak is being deleted completely.
- The design skill itself still documents streaks, badge celebrations, sound/confetti, and all-caps hero buttons. It should eventually be refreshed by Ludvig/Claude so future design sessions do not receive conflicting direction.

## What's next when you come back

In priority order:
1. Open the profile screen on a small iPhone/Android viewport and at 1.3× font scale; verify the three stat cards and «Knutekategorier» heading row.
2. Review only the Codex diff in `apps/mobile/app/profile.tsx`; keep it separate from the pre-existing Claude configuration/document work.
3. Ask Claude/backend reviewer to plan complete streak removal from the API contract and backend without mixing it into the visual PR.

## Gotchas / things to remember

- V1 is a product reference, not code or feature truth. Preserve the core loop; do not revive its sounds, confetti, streak flame, rank-up toast, or achievement celebration components.
- The `badge` word is also used for legitimate status UI such as `18+`, `Tekst`, queue counts, approved/pending/rejected, and gullknute markers. Do not mass-delete those.
- The full Jest suite is slow on this Windows/Expo setup. `pnpm --filter @knuteloop/mobile exec jest --ci --maxWorkers=4` finished successfully in about 78 seconds.
- Jest emitted existing hygiene warnings: `PackSheet.test.tsx` has an update outside `act(...)`, and one worker needed forced teardown. All 85 assertions still passed.
- `design/calm-mobile` was created while the working tree already had Claude/user changes. Branch ownership does not mean Codex authored all visible changes.

## Files touched

- Codex: `apps/mobile/app/profile.tsx`
- Codex: `docs/handoffs/2026-07-24-0146.md`
- Pre-existing modified files left untouched: `.claude/rules/frontend.md`, `.claude/settings.json`, `.gitignore`, `AGENTS.md`, `apps/mobile/CLAUDE.md`, `docs/adr/README.md`, `docs/architecture.md`
- Pre-existing untracked files/directories left untouched: `.agents/`, `.codex/`, `docs/adr/0023-calm-app-no-celebration-layer.md`, `docs/app-store-content-review.md`, older handoffs, `docs/nattplan-2026-07-24.md`, `docs/spotify-for-knuter-plan.md`

## Resume command

When you come back, run this first:

```powershell
git switch design/calm-mobile
git diff -- apps/mobile/app/profile.tsx
pnpm typecheck
pnpm lint
```

## Open questions for Ludvig

- Skal den rolige profilen beholde tre hovedtall som nå, eller vil du senere teste en 2×2-rute med fullført, poeng, plass og gullknuter?
- Skal «Andre på appen» bli stående på egen profil, eller bør sosial oppdagelse primært skje gjennom feeden og topplista?
- Vil du oppdatere `knuteloop-design`-skillens gamle gamification-eksempler sammen med Claude, slik at den nye retningen blir varig?

## Related ADRs

- `docs/adr/0023-calm-app-no-celebration-layer.md` — pre-existing, untracked calm-app decision used as context; not authored or modified by Codex.

## Tests status at session end

- Typecheck: Pass
- Lint: Pass
- Unit tests: Pass — 22 suites, 85 tests
- RLS integration tests: Not run — no backend/database changes
- Notes on test failures: None. Existing Jest teardown/`act(...)` warnings are documented above.
