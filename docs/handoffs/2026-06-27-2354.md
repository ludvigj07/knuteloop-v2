# Handoff — 2026-06-27 23:54

## Snapshot

- **Branch:** `main` (clean except untracked items below)
- **Last commit:** `455dece` `feat(mobile): submit (Send inn) screen in the sticker design (#38)`
- **Uncommitted changes:** None of mine. Untracked, intentionally left: `.agents/`, `.codex/` (other AI-tool config — candidates for `.gitignore`), `docs/app-store-content-review.md` (your App Store cleanup proposal, still awaiting your decision), `docs/handoffs/2026-06-27-0057.md` (the earlier, now-superseded handoff).
- **Session length:** one very long session (continuation of the morning)
- **Claude model:** Claude Opus 4.8

## What got done this session

Big day. Everything is on `main`, all CI-green, full suite green (API 171 + mobile 33 tests).

1. **Sticker design redesign** of the whole knutesjef/admin + knutebibliotek surface — 7 PRs (#25–#31): design-system foundation (fonts, tokens, primitives StickerCard/StickerButton/Chip/Badge/StatTile/Eyebrow/Toast/KnoteIcon), API error-message fix, panel, Bibliotek (browse + region filter + pagination + detail sheet), Knuteboka + folders, create/edit form, and ADR-0017 + frontend.md reconcile.
2. **Press-reliability fix** (#32): StickerCard no longer translates the surface on press (kept taps from registering); LibraryRow/SchoolKnuteRow de-nested the action button.
3. **Local dev unblocked**: created `apps/api/.env` with `DATABASE_URL=postgres://app_user:app_user_dev@localhost:5432/knuteloop_dev` (committed dev creds from migration 0000; postgres-17 superuser is postgres/postgres). `pnpm dev:all` works → seeds 186 library knuter + both schools.
4. **Adversarial bug scan** (multi-agent workflow): 26 candidates → 10 confirmed, 16 refuted. Fixed all 10 across 4 PRs (#33–#36): B1 sensitive-knute gate (folder OR 18+ OR text), B2 folders error state, B3 font-load error, B4 keyboard avoidance, B5 folder double-submit, B6 edit stale-seed, B7 bokmål-400 + maxLength, B8 search debounce, B9 StatTile overflow, B10 token literals.
5. **Web add fix** (#37): the library "+" routed sensitive knuter through `@gorhom/bottom-sheet`, which doesn't present on react-native-web — so "+" did nothing on web. Now "+" always imports directly; sensitivity stays signalled by amber tint + 18+/Tekst badges; detail sheet (mobile) still reachable by tapping the row.
6. **Submit screen committed** (#38): the local Codex-drafted "Send inn" flow (text-vs-photo, lock, ~1MB compression, animated success) + KnutePreviewCard + PhotoEvidence.

## What's in progress

Nothing half-finished. All work is merged and green.

## What's blocked / waiting

- **`docs/app-store-content-review.md`** — your proposal to trim the library for App Store (remove Sex folder etc.) still awaits your go.
- The **hard "are you sure" friction** for adding sensitive knuter was dropped (#37) because both `@gorhom/bottom-sheet` and `Alert.alert` are unreliable on react-native-web. If you want it back cross-platform, it needs a small custom plain-RN confirm modal (see Gotchas).

## What's next when you come back

In priority order:
1. Test the submit + library flows end-to-end on a **device** (not just web) — the detail sheet (@gorhom) only works on native.
2. Optional: build a cross-platform confirm modal so sensitive-add friction works on web too (if you still want that governance step).
3. Optional cleanup: `.gitignore` the `.agents/` + `.codex/` dirs.
4. The App Store content-review decision (`docs/app-store-content-review.md`).

## Gotchas / things to remember

- **You test on web (`localhost:8081`).** `@gorhom/bottom-sheet` (detail/confirm sheets) and `Alert.alert` are **unreliable on react-native-web** — never gate a core action behind them. For cross-platform friction, build a plain-RN overlay (View + Pressable). Saved to memory (`ludvig-tests-on-web`).
- **StickerCard press feel** = shadow fades + slight dim + hitSlop; the surface intentionally does NOT move (moving it cancelled taps). Don't reintroduce a press translate.
- **Two-look app** (ADR-0017): admin/library = sticker; feed/leaderboard/profile/tab bar = older base tokens. Promoting the rest is a token swap, not a rewrite.
- **`isSensitiveKnute(knute)`** (lib/knute-ui) = sensitive folder OR minAge>=18 OR evidenceType 'text' — drives the amber tint + detail-sheet warning.
- `.env` is hook-protected: Claude **cannot** create/edit it (Write + bash + PowerShell all blocked). You create/edit `.env` files by hand.

## Files touched

Too many to list individually — see `git log --oneline be73854..455dece` (PRs #25–#38). Net: a new sticker design system under `apps/mobile/components/primitives` + `components/{library,folder,knute,submit}`, restyled `app/admin/*` + `app/knute/[id].tsx`, `lib/{theme,api,knute-ui}.ts`, `app/_layout.tsx`, `docs/adr/0017`, `.claude/rules/frontend.md`.

## Resume command

```
git checkout main
git pull
pnpm install
pnpm dev:all     # seeds DB + starts API + mobile (apps/api/.env already exists)
```

## Open questions for Ludvig

- Want the cross-platform sensitive-add confirm modal built?
- App Store content trim — approve `docs/app-store-content-review.md`?
- `.gitignore` the `.agents/` + `.codex/` dirs?

## Related ADRs

- **ADR-0017** — the sticker design system (created earlier this session).

## Tests status at session end

- Typecheck: **pass** (all 3 packages)
- Lint: **pass**
- Unit + integration: **pass** — API 171, mobile 33
- RLS integration: **pass** (untouched — frontend-only session)
- Notes: None
