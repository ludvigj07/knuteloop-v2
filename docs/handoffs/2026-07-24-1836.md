# Handoff — 2026-07-24 18:36

## Snapshot

- **Branch:** `feat/schema-branded-ids` (én av FIRE ferdige, upushede brancher — se under)
- **Last commit:** `cb9e1db feat(api): branded SchoolId på skjemakolonnene`
- **Uncommitted changes:** Kun denne handoff-fila (untracked)
- **Session length:** Dagsøkt ~4 t autonom /loop-kjøring (Ludvig hos kompis) + formiddagens Codex-review
- **Claude model:** Claude Fable 5

## What got done this session

**Formiddag — validering av Codex-goalet (elev-hjem):**

- Multi-linse review av `design/elev-hjem` (nytt elev-hjem på `/`, katalog flyttet
  byte-tro til `/knuter`, fem-fane tabbar). Ingen kritiske funn; ~10 reelle småfeil
  rettet: refresh-feil kaster ikke lenger rendret innhold (`error && !data`-regelen),
  tabbar-etikett uten web-ubrukelig font-krymping, flexShrink mot 1,3×-overflyt,
  tekst-bevis viser sitat, `accessible` på gruppert a11y-label, bokmål-fiks,
  `router.replace` til fane-røtter, ny `HomeTopThree.test.tsx` (6 tester).
  Committet som `6718bf2` på `design/elev-hjem`. 25 suiter / 98 mobiltester grønne.

**Dagsplan (fire brancher, alle fra `38cdb9a`, alle committet, INGEN pushet):**

1. **`chore/app-store-vask`** (`d0c0c49`) — N5: biblioteket vasket for App Store.
   75 knuter fjernet (hele Sex- + Alkohol-mappa, tobakk/snus/narkotika, vold/tyveri,
   naken/intim, kroppsmod, hele 50/50-gruppa — alt godkjent av Ludvig 24. juli).
   **186 → 111 knuter**, seed + `docs/library-seed-source.md` i sync, dev-setup-vaktene
   oppdatert (111/0/0/3), `docs/app-store-content-review.md` stemplet UTFØRT.
2. **`feat/fjern-streak`** (`ef33aa9`) — N2: streak HELT ut per ADR-0023.
   `lib/streak.ts` + 10 tester slettet, me-ruta renset, profilens StatTrio → StatDuo,
   mobil-typene synket, v1-spec §2/§12 flagget [V2 SUPERSEDES: ADR-0023].
3. **`feat/dagens-knute`** (`31b0af6`) — N4 backend: `GET /api/knuter/dagens` —
   deterministisk dagsvalg (v1-spec §3 MED Oslo-fiksen), aldersgate (mindreårig får
   aldri 18+-pick), ren pick-funksjon + 10 nye tester (datouavhengige asserts),
   `fetchDagensKnute` i mobil-klienten. **Hjem-kortet er IKKE bygget** — det skal
   inn på elev-hjem-skjermen som venter på merge + visuell QA.
4. **`feat/schema-branded-ids`** (`cb9e1db`) — N7: `.$type<SchoolId>()` på
   `schools.id` + alle sju `school_id`-kolonner (rent typenivå, ingen migrasjon),
   12 testfiler retypet, `declaration: false` for API-appen (fikser TS4023 uten å
   røre `lib/ids.ts`).

**Rank-titler (N3 fra nattplanen) viste seg allerede bygget** (`lib/rank-titles.ts`
+ leaderboard-API) — strøket fra planen.

## What's in progress

- Ingenting halvferdig i kode. Alle fire brancher er grønne og committet.
- `design/elev-hjem` venter fortsatt på Ludvigs visuelle QA (320–360 px + 130 %
  zoom på http://localhost:8081) før Codex-goalet kan markeres complete.

## What's blocked / waiting

- **Push + PR + merge:** push-tillatelser er tilbake i «spør alltid», så alle fire
  brancher (+ `docs/calm-app` og `design/elev-hjem`) ligger kun lokalt. Ludvig må
  si «push» — da lages PR-er og merges i riktig rekkefølge.
- **Merge-rekkefølge som unngår konflikter:** `docs/calm-app` FØRST (alle
  dagsbranchene er basert på dens topp `38cdb9a`), deretter app-store-vask →
  fjern-streak → dagens-knute → schema-branded-ids → elev-hjem (etter QA).
- **`design/calm-mobile`** (`bdaad23`, eldre Codex-branch) overlapper med
  streak-fjerningen i profile.tsx — vurder å droppe den eller rebase manuelt.
- Lokal dev-DB på denne PC-en mangler fortsatt migrasjon 0021 (superbruker-
  kommando i handoff 2026-07-24-0043; ufarlig inntil videre).

## What's next when you come back

1. Ludvig: visuell QA av elev-hjem (5 min i nettleser) + si «push».
2. Push alle brancher, opprett PR-er, CI grønn, merge i rekkefølgen over.
3. Bygg dagens knute-KORTET på hjem-skjermen (etter at elev-hjem + dagens-knute
   er merget — ~30 min, komponentmønstrene finnes i `components/home/`).
4. Kjør `pnpm dev:setup` lokalt etterpå (seeden endret: 186 → 111).
5. Badges-designøkta (felles med Ludvig) når han har lyst — ADR-0023 parkerte dem.

## Gotchas / things to remember

- **Nattens docs-commits (`152b528`–`38cdb9a`) ligger på branchen `docs/calm-app`,
  IKKE på main** — `origin/main` står fortsatt på `bcfae17`. Basér nye brancher på
  `38cdb9a` (eller vent til calm-app er merget).
- App Store-vasken etterlater **0 tekst-knuter og 0 18+-knuter i biblioteket** —
  evidence-type-/aldersgate-featurene er nå seed-tomme (koden består, med tester).
- `declaration: false` i `apps/api/tsconfig.json` er et bevisst valg (TS4023 +
  ubrukt .d.ts) — ikke «rett det tilbake».
- dev-setup-vaktene (`111/0/0/3`) MÅ oppdateres hvis seeden endres igjen.
- pre-bash-hooken blokkerer fortsatt kommandoer som inneholder «main» → bruk
  commit-hash eller `origin/HEAD` som ref.

## Files touched

Se `git log --stat` per branch: `6718bf2` (17 filer), `d0c0c49` (4), `ef33aa9` (9),
`31b0af6` (5), `cb9e1db` (20).

## Resume command

```
git fetch origin && git branch -v
# Etter Ludvigs OK: push docs/calm-app først, så dagsbranchene, PR-er mot main.
pnpm typecheck && pnpm lint && pnpm test
```

## Open questions for Ludvig

- Knutesjef-badge «privat» i godkjenn-køen — ja/nei? (ADR-0021, lener mot ja.)
- Knutesjef-inngang: hjem-kortet som eneste dør, eller fast fane? (/admin er URL-only.)
- Skal `design/calm-mobile` droppes til fordel for streak-fjerningen?

## Related ADRs

- ADR-0023 (calm app) — håndhevet i kode denne økta (streak ut, v1-spec flagget).
- ADR-0015 (aldersgate) — respekteres av dagens-knute-endepunktet.

## Tests status at session end

- Typecheck: **pass** (alle brancher)
- Lint: **pass** (alle brancher)
- Unit/integration: **pass** — per branch: app-store-vask API 248/248 · fjern-streak
  API 237/237 + mobil 85/85 · dagens-knute API 258/258 · schema-branded-ids API
  248/248 · elev-hjem mobil 98/98
- RLS integration tests: **pass** (kjørt i full suite på alle backend-brancher)
- Notes on test failures: Ingen.
