# Handoff — 2026-06-12 21:11

## Snapshot

- **Branch:** main (everything merged, all feature branches deleted)
- **Last commit:** 65b7171 Add /api/feed + TikTok-style fullscreen feed (#3)
- **Uncommitted changes:** No (this handoff file only)
- **Session length:** ~2.5 hours total (third and final handoff today; see also
  2026-06-12-2019.md)
- **Claude model:** Claude Fable 5

## What got done this session (since the 20:55 handoff)

- **Created `AGENTS.md`** in the repo root — design-only rules for non-Claude
  AI tools. Ludvig plans to use Codex for visual design (colors/layout) in
  `apps/mobile/`. The file hard-blocks backend, .env, dependencies, and agent
  config; mandates theme-token discipline, bokmål, WCAG AA, no third-party
  anything. Ludvig also has a paste-ready kickoff prompt (in the session, not
  in the repo).
- **Merged everything to main on Ludvig's explicit decision, WITHOUT brother's
  review** (he chose this knowingly after being warned — see Gotchas):
  - PR #1 (knutesjef-panel) → squash-merged as `8fda795`
  - PR #2 (feed) was auto-CLOSED by GitHub when #1's branch was deleted
    (stacked PR pitfall) → recreated as PR #3 from rebased branch
    `feat/feed-v2` → squash-merged as `65b7171`
  - All feature branches deleted, local and remote. Only `main` remains.

## What's in progress

None. Clean tree, everything shipped.

## What's blocked / waiting

- **Brother's review now happens POST-merge.** Both #1 and #3 contain backend
  code he hasn't seen. Ask him to review the two squash commits on main
  (`8fda795`, `65b7171`) — the PR descriptions contain the review notes
  (timestamp-cursor trade-off, ?all=1 role-gating question, picsum dev images,
  rate-limiting still missing project-wide).

## What's next when you come back

1. Get brother's post-merge review of `8fda795` and `65b7171`. If he flags
   something, fix-forward in a new PR (never rewrite main).
2. Likely next build: **Bunny image upload** (signed upload URLs) — makes the
   feed real. Before starting, decide if video ships in the same work; if yes,
   write the ADR superseding 0009 FIRST.
3. Ludvig may run Codex design passes meanwhile — check `design/*` branches.

## Gotchas / things to remember

- **Post-merge review debt exists.** Merging without backend review was
  Ludvig's explicit call (2026-06-12, end of session). Don't treat it as
  precedent: the default pipeline is still PR → brother review → merge.
- **Stacked-PR pitfall learned today:** `gh pr merge --delete-branch` on the
  base PR CLOSES any PR stacked on it (GitHub won't retarget when the base
  branch dies). Next time: retarget the stacked PR to main BEFORE merging the
  base, or don't delete the branch until the stack is empty.
- `gh pr merge --delete-branch` also deletes your LOCAL copy of the branch.
- The feed seeds show picsum.photos test images (dev only). Re-seed with
  `pnpm --filter @knuteloop/api dev:seed-feed` (idempotent).
- Vipps login + video decisions still live only in Claude's memory — two ADRs
  owed (superseding 0006 and 0009) before either gets built.

## Files touched (this handoff segment)

- AGENTS.md (new, 85 lines — merged via PR #3)
- docs/handoffs/2026-06-12-2111.md (this file)

## Resume command

When you come back, run this first:

```
/comeback
git log --oneline -5        # see the two squash merges
pnpm dev:all                # http://localhost:8081 → "Feed"
```

## Open questions for Ludvig

- Has brother reviewed `8fda795` + `65b7171` yet? Chase if not.
- Codex design work: any `design/*` branches to review and merge?

## Related ADRs

None written. Two owed (Vipps ⊃ 0006, video ⊃ 0009) before respective builds.

## Tests status at session end

- Typecheck: pass
- Lint: pass
- Unit tests: pass (67/67)
- RLS integration tests: pass
- Notes on test failures: none
