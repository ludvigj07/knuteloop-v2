# Handoff — 2026-07-03 00:33

## Snapshot

- **Branch:** main, ren og synket med origin. **0 åpne PR-er, 0 uncommitted.**
- **Siste commit:** 9d820ad `perf(mobile): memoize PendingCard … (#71)`
- **Session length:** ~1,5 t (startet som en `/handoff`, ble til opprydding + PR-1 av design-epic)
- **Claude model:** Opus 4.8 (1M context)

## What got done this session

1. **Branch-opprydding:** slettet 6 gamle remote-branches som alt var i main (juni-utkast erstattet av juli-serien). `git fetch --prune` fjernet 15 døde tracking-refs i tillegg. GitHub har nå BARE `main`. SHA-ene ble notert i samtalen om noe må gjenopplives.
2. **Ny plan lagt:** «Fullfør sticker-designen»-epic — review + toppliste + profil var de siste skjermene på det gamle base-laget. Ludvig valgte denne som hovedjobb (fremfor varsler / prod-klargjøring).
3. **PR-1 (#70) — review-skjermen i sticker:** trakk ut `PendingCard` → `components/review/`, sticker-kort/knapper/chip, skeleton i stedet for spinner, sticker feil/tom-states, `success`-haptikk, toast ved feil, FlatList. Krympet skjermen 309→220 linjer. +6 behavior-tester.
4. **PR-2-lite (#71) — perf-fiks:** `React.memo` på `PendingCard` + stabile `approve.mutate`/`reject.mutate`-handlere + FlatList `extraData`, så ett godkjenn-trykk re-renderer bare det ene kortet, ikke hele køen.

## Kontekst du bør huske: appens faktiske tilstand (verifisert i koden i natt)

- **Kjerneløkka funker ende-til-ende i DEV:** bla → send inn (kamerabilde+komprimering, eller tekst) → knutesjef godkjenner → feed/toppliste/profil.
- **Design (sticker):** ferdig på hjem, feed, bibliotek, knuteboka, knutesjef-faner, send-inn, OG NÅ review. **Igjen: toppliste + profil.**
- **Bildelagring:** dev-lokal funker; Bunny (prod) er 1 TODO unna i `apps/api/src/lib/storage.ts` + krever konto/nøkler (penger).
- **Auth:** dev-innlogging (identity switcher). Ekte (Vipps/Apple) bevisst parkert ~1 år.
- **Push-varsler:** IKKE bygget. Submit-skjermen lover «du får varsel når den er godkjent» — tomt løfte foreløpig.
- **Gamification** (badges/streak-bonus/rank-titler/Wrapped): ikke bygget, bevisst parkert (press-fri).

## Gotchas / things to remember

- **Perf-tallene fra web lyver.** Ludvig profilerte `expo start --web` og så 1–3,5 s INP på trykk. Det er dev-web (React dev mode + Reanimated på hovedtråden + Metro re-bundle), IKKE representativt for native. #71 var en ekte liten forbedring, men **ikke jag web-INP videre** — mål på en telefon (native dev-build) hvis du vil vite reell respons.
- **Toppliste-fila er 519 linjer — over 500-grensa (regel 10).** PR-2 må dele den opp (`LeaderRow` + topp-3-podium + «din rad»), ikke bare restyle. Profil er 489 linjer, samme grep.
- **Konvensjon bekreftet:** tester ligger ved KOMPONENTER, ikke skjermer (ingen route-tester i `app/`). Trekk ut testbare komponenter og test dem.
- Sticker-primitivene: `StickerCard`, `StickerButton` (variants: primary/accent/secondary/destructive/ghost), `Chip` (tones), `Text` (font: sans/display/mono), `Skeleton`, `Toast`/`useToast`, `KnoteIcon`. Tokens i `sticker`-namespace i `lib/theme.ts`. Se `app/feed.tsx` som mal.

## What's next when you come back

1. **PR-2 — toppliste (`app/leaderboard.tsx`) til sticker + del opp.** Den store. Friskt hode anbefales.
2. **PR-3 — profil (`app/profile.tsx`) til sticker + del opp.** Da er design-epic-en lukket.
3. Deretter: hovedvalget var varsler (Expo Push) vs. prod-klargjøring (Bunny+deploy) — diskuter på nytt, prioriteringer kan ha flyttet seg.

## Resume command

```
git checkout main
git fetch --prune && git pull
pnpm install
git checkout -b feat/leaderboard-sticker
# les app/leaderboard.tsx (519 linjer) + app/feed.tsx (mal) + lib/theme.ts sticker-namespace
```

## Tests status at session end

- Typecheck: pass · Lint: pass · Mobil-tester: **63 grønne** (18 suiter). API ikke kjørt (ingen backend-endring i natt).

## Related ADRs

- Ingen nye. Arbeidet følger ADR-0017 (sticker-designsystemet).

## Note

Økta endte hyggelig — Ludvig var litt full og broren Brage var innom, så vi stoppet kodingen bevisst. Riktig valg: ingen merge/koding uten at Ludvig kan reviewe. Alt er i ren tilstand.
