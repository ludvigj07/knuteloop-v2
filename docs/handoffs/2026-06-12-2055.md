# Handoff — 2026-06-12 20:55

## Snapshot

- **Branch:** feat/feed
- **Last commit:** cac9ad0 dev-seed-feed: drop stale comment + unused import
- **Uncommitted changes:** No — working tree clean
- **Session length:** ~2 hours (second handoff this session; first at 20:19 covered the PR #1 work)
- **Claude model:** Claude Fable 5

## What got done this session

- **Built the feed feature end-to-end on new branch `feat/feed`** (branched off
  `feat/knutesjef-panel` — it's a stacked PR):
  - Backend: `GET /api/feed` in `apps/api/src/routes/feed.ts` — the school's
    approved submissions, newest first, cursor-paginated (ISO timestamp cursor,
    limit 1–50 default 20), joined with russenavn + knute title/points in one
    query. Mounted in `app.ts`.
  - 7 integration tests in `feed.test.ts`: 401, happy path, pending/rejected
    exclusion, cross-tenant isolation both directions, 3-page cursor walk,
    malformed cursor → 400, oversize limit → 400.
  - Mobile: `apps/mobile/app/feed.tsx` — TikTok-style fullscreen vertical-paging
    feed. The photo shows WHOLE (`contain`), with a blurred `cover` copy of the
    same image filling the rest of the screen (Ludvig's requirement: nothing
    cropped, unlike v1's small image boxes). Infinite scroll via
    `useInfiniteQuery`. Feed link added on the home screen.
  - Dark feed color tokens added to `apps/mobile/lib/theme.ts`.
  - `pnpm --filter @knuteloop/api dev:seed-feed` — idempotent script that seeds
    8 approved submissions into the RUNNING dev DB (no drop, no token rewrite).
    Uses picsum.photos portrait URLs as dev-only stand-in images until Bunny.
- **Opened PR #2** (stacked on PR #1): https://github.com/ludvigj07/knuteloop-v2/pull/2
  — `/backend-review` ran clean; review notes are in the PR description.
- **Recorded two direction changes in Claude's memory** (NOT yet in ADRs):
  - Login will be **Vipps**, not Entra ID (parked; needs ADR superseding 0006).
  - **Video is coming to v2** after improved funding; Ludvig says legal is
    solved. Needs ADR superseding 0009 + CLAUDE.md rule 11 update BEFORE building.

## What's in progress

None mid-flight — the feed feature is complete, tested, and pushed. Both PRs
await brother's review.

## What's blocked / waiting

- **PR #1** (knutesjef-panel) — waiting on brother's backend review.
- **PR #2** (feed) — stacked on #1; GitHub retargets it to main when #1 merges.
- Real images in the feed wait on **Bunny storage integration** (signed upload
  URLs) — not started.

## What's next when you come back

1. Check PR #1 and #2 status: `gh pr list`. Merge order: #1 first (squash, in
   GitHub UI), then #2.
2. If brother flagged the timestamp-cursor or picsum notes on #2, address them.
3. Next feature discussion: Bunny image upload (makes the feed real) was the
   natural next step before video/Vipps. Discuss with Ludvig — priorities may
   have shifted.

## Gotchas / things to remember

- **PR #2 is STACKED on PR #1.** Don't merge #2 first. After #1 squash-merges
  and its branch is deleted, GitHub auto-retargets #2 to main.
- **Feed images are picsum.photos test data, dev seed only.** The feed renders
  `<Image>` only when imageKey starts with http(s); otherwise a 📸 placeholder
  card. Real images come with Bunny.
- **dev:seed-feed is idempotent** — re-running replaces the previous batch and
  SETS Frida's points absolutely (doesn't increment).
- **Timestamp-only cursor** in /api/feed can skip an item on identical
  microsecond timestamps — known, documented, fine for a feed.
- `gh` CLI is now installed (`C:\Program Files\GitHub CLI\gh.exe`, logged in as
  ludvigj07). Open bash sessions may need the full path; new terminals find it.
- Video + Vipps decisions live in Claude's memory but NOT in the repo yet —
  write the superseding ADRs when work starts on either.

## Files touched

- apps/api/package.json (+1 script)
- apps/api/scripts/dev-seed-feed.ts (new, 121 lines)
- apps/api/src/app.ts (+2, mount feed route)
- apps/api/src/routes/feed.ts (new, 83 lines)
- apps/api/src/test/integration/feed.test.ts (new, 194 lines)
- apps/mobile/app/feed.tsx (new, 309 lines)
- apps/mobile/app/index.tsx (+11, feed nav link)
- apps/mobile/lib/api.ts (+17, fetchFeed + types)
- apps/mobile/lib/theme.ts (+6, dark feed tokens)

## Resume command

When you come back, run this first:

```
/comeback
gh pr list
pnpm dev:all          # then open http://localhost:8081 → "Feed"
```

## Open questions for Ludvig

- Vipps Login requires BankID — can 17-year-old russ use it? Verify with Vipps
  docs before the auth ADR.
- Should the feed eventually filter (e.g., only people you follow / your
  class), or stay one school-wide stream? Affects future schema.
- Video: max length? File size cap? These drive the Bunny + ADR work.

## Related ADRs

None written this session — but TWO are now owed:
- ADR superseding 0006 (Entra ID → Vipps) before auth work starts.
- ADR superseding 0009 (photos only → photos + video) before video work starts.

## Tests status at session end

- Typecheck: pass
- Lint: pass
- Unit tests: pass (67/67)
- RLS integration tests: pass
- Notes on test failures: none
