# Handoff — 2026-07-02 19:21

## Snapshot

- **Branch:** `feat/startpakke-sheet` (D5 — last slice of the demo plan, in PR when you read this)
- **Session length:** full day (the first Fable 5 session; gh CLI now authenticated on the main PC)
- **Everything else from today is MERGED to main** (#49–#63)

## What got done today (the short version of a very long day)

1. **Docs-sync + cross-PC map** (#45/#50) — recovered the lost cabin handoff; docs now describe the built library.
2. **Sticker design on the student screens**: hjem + feed (#46/#52) with folder chips (ADR-0018 for students).
3. **Bibliotek B1** (#53): dense one-panel catalog with descriptions, pinned search/chips, region behind one button.
4. **Knutesjef-verden** (#56): own tab bar Kø | Biblioteket | Knuteboka with pending-badge; Knutesjef tab lands in the queue.
5. **Mappa som arbeidsbenk** (#57): empty-folder dead end fixed, add-own-knuter sheet, create-knute-here, Mapper multi-select in the form.
6. **Tilgjengelige | Fullført** (#58/#59): student catalog is a to-do list by default; segmented control with counts; instant status refresh after submit/approve/reject (myStatus backend join).
7. **Demo-planen D1–D5** (from Ludvig's HTML demo, all decisions locked in memory `knutesjef-operating-vision`):
   - D1 (#60): add-sheet v2 — theme suggestion pre-checked («Foreslått», «Ny mappe: <tema>» when missing), minst-én rule, locked Alle-row, ✏️ edit-before-save, folder-naming toast.
   - D2 (#61): from-folder mode — context banner + pre-checked folder.
   - D3 (#62): Knuteboka structure — Alle-hero («havner her uansett»), one-panel folder rows, dashed Ny mappe, workbench actions on TOP.
   - D4 (#63): folderIds per knute in GET /api/knuter + importedKnuteId in library browse (backend, labeled `etter-review-bror`); folder chips in Alle-view; ✓ opens MANAGE mode (change folders/edit copy).
   - D5 (this branch): GET /api/library/packs/:id (contents + per-school imported flags, `etter-review-bror`) + PackSheet — see-before-you-add, «Legg til N nye».

## Process changes today (IMPORTANT)

- **Brother is unavailable until ~mid-July.** Backend merges WITHOUT pre-review; every backend PR is labeled **`etter-review-bror`** (filter: #41, #42, #49, #58, #63, D5). Claude's /backend-review + full test suites gate each one. Revert to review-before-merge when he's back.
- gh CLI authenticated on the main PC → Claude runs the whole PR/merge pipeline (squash after green CI, per the rules).

## Test state at session end

- API: 182/182 (real local Postgres — `knuteloop-pg` container, auto-starts with Docker Desktop now)
- Mobile: 55/55, typecheck + lint clean across the workspace

## What's next

1. Merge D5 if CI was green (Claude does this automatically in-session).
2. **Ludvig verifies the whole knutesjef flow visually** — the point of the day: pakke-sheet → add-sheet with forslag → fill a folder from inside it → manage via ✓ → Alle-view chips.
3. Later backlog (unchanged): rest of design epic (toppliste, profil, review-restyle), badges/dagens-knute decisions, sponsor epic, auth (parked ~1 yr).

## Gotchas

- `knuter.category` is still legacy-hidden (ADR-0018) — don't surface it.
- AddToFolderSheet has TWO modes (add/manage) — key resets per `${mode}-${id}`.
- Pack import auto-files by suggestedFolder (BY DESIGN — «Knuteloops anbefaling»); single import never does.
- FlashList + segmented-panel rows: first/last caps via isFirst/isLast — pass extraData when row-external state drives rendering.

## Resume command

```
git checkout main && git pull && pnpm install && pnpm dev:all
```
