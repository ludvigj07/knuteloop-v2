# Handoff — 2026-07-24 04:00

## Snapshot

- **Branch:** `design/elev-hjem`
- **Last commit:** `38cdb9a docs: handoff etter opprydding 2026-07-24`
- **Uncommitted changes:** Yes — `apps/mobile/app/_layout.tsx`, `apps/mobile/app/feed.tsx`, `apps/mobile/app/index.tsx`, `apps/mobile/app/knuter.tsx`, `apps/mobile/components/AppTabBar.tsx`, `apps/mobile/components/AppTabBar.test.tsx`, `apps/mobile/components/home/*`, `apps/mobile/components/knute/KnuteCatalogStates.tsx`, `apps/mobile/lib/theme.ts`, and this handoff.
- **Session length:** ~0.5 hours
- **Codex model:** Codex (GPT-5 family; exact UI model name not exposed)

## What got done this session

- Replaced the knute catalog at `/` with a real student home screen in `apps/mobile/app/index.tsx`, using only existing `/api/me`, feed, leaderboard, knute, and pending-count data.
- Built a calm, social home hierarchy: personal greeting, latest approved school activity, compact personal overview, explicit catalog entry, current school top three, and a role-gated knutesjef entry.
- Preserved the complete existing student catalog as its own `/knuter` route, including search, folders, available/completed views, random choice, and the original detail flow.
- Reworked `AppTabBar` into five clear student destinations (`Hjem`, `Knuter`, `Øyeblikk`, `Toppliste`, `Profil`) with project press/haptic behavior, safe-area-aware horizontal placement, and text fitting for larger system text. The legacy `/admin` panel still gets an active knutesjef destination; the normal knutesjef entry now appears on home only for `knutesjef`/`admin`.
- Added production loading/error/empty states for home sections and extracted catalog loading/error states that preserve navigation and respect all four safe-area edges.
- Added seven behavioral tests across `HomeActivityCard`, `HomeNavigationCards`, and `AppTabBar`; the mobile suite is now 24 suites / 92 tests.
- A continuation audit corrected singular Bokmål for one available knute and stopped the home preview from presenting zero-point users as an active top three before the school has real scoring activity.
- Verified the Expo web bundle builds successfully from the live dev server (`HTTP 200`, 11,271,720-byte development bundle).
- Confirmed `pnpm typecheck`, `pnpm lint`, and all mobile tests pass. No backend, auth, API-client, shared, dependency, package, environment, ADR, or Claude-configuration files were changed.

## What's in progress

- File: `apps/mobile/app/index.tsx` and `apps/mobile/components/home/*`
- What's done in this file: Implementation, responsive code review, accessibility wiring, reduced-motion handling, tests, typecheck, lint, and Expo web bundling.
- What's not done: Required visual inspection at a small mobile viewport and 1.3× system text, followed by any visual fixes discovered there.
- What state is it in: Compiles and all automated checks pass; no TODO markers. The explicit Codex goal is `blocked`, not complete, because visual QA is still unperformed.

## What's blocked / waiting

- The in-app Browser connector returned no available browser instances (`agent.browsers.list()` was empty) in three consecutive goal turns, so the required browser screenshots and interaction pass could not be performed.
- This Windows machine has no `adb` or Android emulator installed in any of those three attempts, so there is no simulator fallback available. Expo web and API are both listening locally, but controlling them through an unrelated browser mechanism is disallowed by the loaded Browser skill.
- After the third identical audit, the explicit Codex goal was correctly marked `blocked`. It was not marked `complete`.

## What's next when you come back

1. Retry the in-app Browser connector (or attach an Android simulator), open `http://localhost:8081`, and select a seeded dev identity if the token has expired.
2. Inspect home and `/knuter` at approximately 320–360 px width, then repeat at 1.3× text. Check the five-item bottom bar, latest-activity card, three-stat row, top-three rows, long russenavn/point values, scrolling, and every loading/error/empty state.
3. Fix any visual defects, rerun `pnpm typecheck`, `pnpm lint`, and `pnpm --filter @knuteloop/mobile exec jest --runInBand --forceExit`, then update this handoff and mark the active Codex goal complete.

## Gotchas / things to remember

- No `/api/knuter/dagens` contract exists in the current API/mobile client. Per the task and nattplan, dagens knute was deliberately not faked or wired.
- Home uses `['feed', 'home']` rather than `['feed']` because the fullscreen feed stores `InfiniteData` under `['feed']`; sharing the exact query key with a single-page response would corrupt the cache shape. Prefix invalidation of `['feed']` still refreshes both.
- The existing Jest suite leaves asynchronous handles open and prints older `act(...)` warnings from `PackSheet`/`KnutesjefTabBar`. All 91 tests pass; `--forceExit` is currently needed for a clean command exit.
- `git diff --stat` does not include the new untracked route/components until they are staged. Do not mistake the large `index.tsx` deletion for lost catalog code — the original catalog is preserved in untracked `apps/mobile/app/knuter.tsx`.
- `apps/mobile/app/profile.tsx` still contains pre-existing streak UI despite ADR-0023 and was intentionally not touched in this single-purpose home-screen branch.

## Files touched

- `apps/mobile/app/_layout.tsx`
- `apps/mobile/app/feed.tsx`
- `apps/mobile/app/index.tsx`
- `apps/mobile/app/knuter.tsx`
- `apps/mobile/components/AppTabBar.tsx`
- `apps/mobile/components/AppTabBar.test.tsx`
- `apps/mobile/components/home/HomeActivityCard.tsx`
- `apps/mobile/components/home/HomeActivityCard.test.tsx`
- `apps/mobile/components/home/HomeNavigationCards.tsx`
- `apps/mobile/components/home/HomeNavigationCards.test.tsx`
- `apps/mobile/components/home/HomeOverviewCard.tsx`
- `apps/mobile/components/home/HomeScreenStates.tsx`
- `apps/mobile/components/home/HomeTopThree.tsx`
- `apps/mobile/components/knute/KnuteCatalogStates.tsx`
- `apps/mobile/lib/theme.ts`
- `docs/handoffs/2026-07-24-0400.md`

## Resume command

When you come back, run this first:

```powershell
git switch design/elev-hjem
git status --short
pnpm --filter @knuteloop/mobile start --web
```

## Open questions for Ludvig

- None. Product and design choices are implemented; only objective visual QA is pending.

## Related ADRs

- `docs/adr/0017-sticker-design-system.md`
- `docs/adr/0018-folders-as-primary-axis.md`
- `docs/adr/0023-calm-app-no-celebration-layer.md`

## Tests status at session end

- Typecheck: Pass — `pnpm typecheck`
- Lint: Pass — `pnpm lint`
- Unit tests: Pass — 24 suites / 92 tests via `pnpm --filter @knuteloop/mobile exec jest --runInBand --forceExit`
- RLS integration tests: Not run — no backend/database changes
- Notes on test failures: None. Existing asynchronous-handle warning requires `--forceExit`; existing `act(...)` warnings remain in unrelated older tests.

---

## Addendum — Claude-review (2026-07-24, senere samme natt)

Claude Fable kjørte uavhengig multi-linse review av hele diffen og verifiserte
Codex' påstander selv: typecheck/lint/testene var reelle, katalog-flyttingen til
`/knuter` er byte-tro (maskin-diffet mot HEAD), ingen ADR-0023-brudd, og
query-nøkkel-splitten `['feed']` vs `['feed','home']` er trygg (ingen writers,
prefix-invalidering treffer begge).

**Rettelser etter reviewen (committet på denne branchen):**

1. `app/index.tsx` — fullskjerm-feil kun når ingenting kan vises (`error && !data`,
   samme regel som knuter.tsx dokumenterer): en mislykket pull-to-refresh kaster
   ikke lenger hele det rendrede hjemmet. Hjem-kortene bruker nå `router.replace`
   til fane-røtter (matcher tabbarens semantikk; unngår dobbel web-historikk).
2. `AppTabBar.tsx` — aktiv-pillen bruker `minWidth` i stedet for fast bredde, og
   font-krympingen på etiketten er fjernet (`adjustsFontSizeToFit` er no-op på
   react-native-web → «Topplis…» i nettleseren der Ludvig tester).
3. `HomeActivityCard.tsx` — `flexShrink` på seksjonstittel og russenavn (overflyt
   ved 1,3× / lange navn); feil-gren vises kun uten cachet innhold; tekst-bevis
   viser sitatet (`caption ?? knuteTitle`) i stedet for «Bildet er ikke
   tilgjengelig ennå.»; tekst-brønnen bruker `minHeight` (PendingCard-konvensjonen,
   klipper ikke ved 1,5×).
4. `HomeTopThree.tsx` — feil-gren kun når ingen cachede rader finnes.
5. `HomeOverviewCard.tsx` — `accessible` på stats-raden så den komponerte
   skjermleser-teksten faktisk annonseres på iOS/Android (profile.tsx-mønsteret).
6. `HomeScreenStates.tsx` — «Kunne ikke åpne hjemmet» → «Kunne ikke laste innholdet».
7. Ny `HomeTopThree.test.tsx` — 6 atferdstester (rad-navigasjon, Deg-chip,
   tom/feil/retry, cached-rader-ved-feil, toppliste-knapp).

**Status etter rettelser: typecheck ✅, lint ✅, 25 suiter / 98 tester ✅.**

**Parkert (bevisst ikke endret — til Ludvig):**
- Knutesjef-fanens `href: '/admin'` er død kode og /admin-panelet er kun nåbart
  via URL (gjaldt også før denne branchen). Produktspørsmål: skal knutesjef ha
  fast fane på alle skjermer, eller er hjem-kortet eneste dør? KNUTESJEF_TABS-
  varianten mangler også Profil.
- `spacing.none` som 0 i logikk — eksisterende repo-konvensjon (leaderboard.tsx
  m.fl.); evt. opprydding er en egen, samlet jobb.
- Visuell QA ved 320–360 px + 1,3× tekst gjenstår (Codex-goalets siste krav —
  goalet skal forbli `blocked` til den er gjort).
