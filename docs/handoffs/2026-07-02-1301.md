# Handoff — 2026-07-02 13:01

## Snapshot

- **Branch:** `docs/sync-docs-with-library` (pushed; PR not opened yet — `gh` not logged in on this PC)
- **Last commit:** `02bf1f1` docs: sync docs with the built library (ADR-0014 reality)
- **Uncommitted changes:** only this handoff file at write time
- **Session length:** ~1 hour
- **Claude model:** Claude Fable 5 (first session after switching from Opus 4.8)

## What got done this session

- **Docs-drift cleanup (the "library is not modeled yet" lie):** `docs/architecture.md` (routes list, data model, §1), `.claude/rules/database.md` §7 (rewritten: school side + library side, import=copy), `docs/glossary.md` (3 rows pointed at tables that never existed), `apps/api/src/db/schema/knuter.ts` (comments only — header + category note). `/backend-review` clean, typecheck + lint green.
- **Recovered the lost 2026-06-26 handoff** — it only existed on the superseded `feat/library-knuteboka` branch, never reached main.
- **Mapped ALL work across the two PCs** (main PC + cabin PC) — see "The map" below. This was the session's real deliverable: Ludvig had lost the overview because the cabin sessions (June 28) never wrote handoffs.
- Ran `pnpm install` after fast-forwarding main (21 new packages from the sticker PRs — typecheck fails without it).

## The map (state of ALL branches, verified 2026-07-02)

**main** = everything through PR #40 (June 28): core loop, library + folders, whole sticker design system (ADR-0017), folder icons. Docs described an older reality until this session's branch.

**Unmerged cabin work — the "Spotify for knuter" stack (June 28). ALL up-to-date with main, merge cleanly:**

| Branch | What it is | Size | Review needs |
|---|---|---|---|
| `docs/adr-folders-primary-axis` (PR-4) | **ADR-0018, Status: Accepted** — folders are THE browse axis; library ＋ = "add to playlist" (pick folders, idempotent, no auto-filing on single import); pack import unchanged; `category` kept hidden until profile rings redesigned | +78, docs only | none — merge first, it's the "why" for PR-1–3 |
| `feat/folder-driven-import` (PR-1) | **Backend:** single import takes `folderIds[]`, idempotent re-import (never 409); rewrites `lib/library-import.ts`, `routes/library.ts` | +276/−132 (174 of it tests) | `/backend-review` + brother |
| `feat/add-to-folder-sheet` (PR-2) | Mobile: library ＋ opens "Legg til i …" folder picker (`AddToFolderSheet.tsx`). **Stacked on PR-1** | +328 own delta | frontend only |
| `feat/student-folder-browse` (PR-3) | Mobile: student "Knuter" tab browses by folder chips ("Alle" + school folders) | +142 | frontend only |

**Stale branches (safe to delete once Ludvig OKs):** `harden/powershell-guards`, `feat/design-system-skill` (both squash-merged as #23/#24), `feat/library-browse-polish`, `feat/library-knuteboka` (superseded by the sticker rebuild #28/#29; their unique handoff file was recovered this session). Local copies of the last two also exist on the main PC.

## What's in progress

- `docs/sync-docs-with-library` — committed + pushed, PR needs to be opened from the web (or after `gh auth login`).

## Priorities update (Ludvig, mid-session 2026-07-02)

Ludvig reset the plan — this OVERRIDES the earlier recommendation in this file:

1. **Merge the docs branch** (safe cleanup, no app logic).
2. **Auth/Vipps is parked ~1 year.** Launch is next russetid (spring 2027). Do NOT spend time on ADR-0016 or the auth epic now.
3. **MAIN JOB this period: unify the WHOLE app in the sticker design (ADR-0017).** Admin side is done. Student-facing screens are still old design. Start with feed/hjem (most used by russ). Goal: one app that looks finished and consistent.
4. **Do NOT build streak-bonus or sound effects** — Ludvig leans toward cutting them (pressure mechanics + minor users); not 100% final. Badges + dagens knute stay on the later-list.
5. **Cabin stack:** backend PR-1 → tee up as its own PR for brother review. Frontend bits (PR-2/PR-3) → fold into the design job. ADR-0018 (PR-4) merges as docs.
6. **Ludvig is full-time (24/7) until 2026-07-28** (military intake). This is the big push window.

## Design-unification status (verified by grep for sticker primitives)

| Screen | File | Status |
|---|---|---|
| Admin panel, Knuteboka, bibliotek, folder, edit | `app/admin/*.tsx` | ✅ sticker |
| Knute-detalj + submit | `app/knute/[id].tsx` | ✅ sticker (PR #38) |
| Hjem (catalog) | `app/index.tsx` | ❌ old — **start here**, fold in PR-3's folder chips |
| Feed | `app/feed.tsx` | ❌ old — **start here** |
| Toppliste | `app/leaderboard.tsx` | ❌ old |
| Profil | `app/profile.tsx` | ❌ old |
| Review-kø (knutesjef) | `app/review.tsx` | ❌ old — Ludvig's list forgot this one |
| Menylinja | `app/_layout.tsx` | ⚠️ does not exist — root is a plain Stack, no tab bar. BUILD it in sticker style, don't "restyle" |
| Dev-login | `app/dev-login.tsx` | ❌ old, dev-only, lowest priority |

## What's blocked / waiting

- **`gh` CLI not logged in on the main PC** (device-flow needs an interactive terminal — Ludvig runs `gh auth login` himself). Until then: PRs open via web compare URLs.
- **Possible unpushed work on the cabin PC** — cannot be seen from here. Next time at the cabin: `git status` + push everything.
- ADR-0011/0012/0013 still Proposed — decide when relevant, no urgency. ADR-0016 (Vipps): parked ~1 year (see priorities above).

## What's next when you come back

1. Merge the docs PR: <https://github.com/ludvigj07/knuteloop-v2/compare/main...docs/sync-docs-with-library>
2. Merge ADR-0018 (docs only, already Accepted): <https://github.com/ludvigj07/knuteloop-v2/compare/main...docs/adr-folders-primary-axis>
3. Open PR-1 for brother: <https://github.com/ludvigj07/knuteloop-v2/compare/main...feat/folder-driven-import> (Claude runs `/backend-review` on it first)
4. **Design epic, screen by screen in small PRs:** hjem + feed first (with PR-3's folder chips folded in), then menylinja (new tab bar), toppliste, profil, review. Load `/knuteloop-design` before each.
5. After PR-1 lands: tiny docs touch-up — `database.md` §7 + `architecture.md` §4 say single import auto-files by `suggested_folder`; ADR-0018 changed that (knutesjef picks folders; only PACK import auto-files).
6. Delete the 6 stale branches (needs Ludvig's OK).

## Gotchas / things to remember

- **Cabin sessions must end with `/handoff` too** — the June-28 marathon (16 merged PRs!) left zero handoffs; that's why the overview was lost.
- After pulling main on any PC: run `pnpm install` before trusting typecheck.
- ADR-0018 was already ACCEPTED at the cabin — don't re-litigate it; the stack implements it.
- `gh` CLI is logged in on NEITHER PC apparently (June-26 handoff said the same).
- Integration tests need Docker (testcontainers) — not running on the main PC right now; CI covers PRs.
- Streak-bonus + sounds: leaning CUT (not final). When the decision firms up, write a small ADR so v1-spec §2's bonus tiers are formally superseded.
- Timeline anchor: **launch = russetid spring 2027; Ludvig in military from 2026-07-28.**

## Files touched

```
.claude/rules/database.md        | 39 +++++++++---------
apps/api/src/db/schema/knuter.ts | 12 ++++---  (comments only)
docs/architecture.md             | 47 ++++++++++++-------
docs/glossary.md                 |  6 ++--
docs/handoffs/2026-06-26-0037.md | 66 +++++++++++++++++ (recovered)
docs/handoffs/2026-07-02-1301.md | (this file)
```

## Resume command

```
git checkout main && git pull && pnpm install
# then open the docs PR if not merged yet:
# https://github.com/ludvigj07/knuteloop-v2/compare/main...docs/sync-docs-with-library
```

## Open questions for Ludvig

- OK to delete the 6 stale branches (4 remote + 2 local)?
- Run `gh auth login` in a terminal window (interactive) so Claude can manage PRs from here?
- Streak-bonus + sounds: final cut-decision before launch (leaning cut; write the ADR then).

## Related ADRs

- ADR-0018 (Accepted, on the unmerged `docs/adr-folders-primary-axis` branch) — read it before touching library/folder code.
- ADR-0017 (sticker design) landed on main June 28 with the design-system skill.

## Tests status at session end

- Typecheck: pass
- Lint: pass
- Unit tests: not run locally (Docker not up) — CI runs them on the PR
- RLS integration tests: not run locally — no schema/functional changes this session
- Notes: the only backend change is two comment blocks in `knuter.ts`.
