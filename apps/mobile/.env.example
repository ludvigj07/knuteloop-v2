# apps/mobile/.env.example — committed template. Copy to apps/mobile/.env (gitignored) for local overrides.

# API base URL — points at local API server during dev.
EXPO_PUBLIC_API_URL=http://localhost:3000
